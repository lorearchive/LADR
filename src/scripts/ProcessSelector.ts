export default function ProcessSelector( NestedArray: (string | number)[][], group: number, TextEn?: string ){

    const lang = TextEn === undefined ? "ko" : "en"



    if (group === 0) {

        let previousGroup = 0 // If group is 0, we are processing a selector. So to prepare for the selections, it is often useful to set the previous group to 0.

        


    } else {

    }

    

}